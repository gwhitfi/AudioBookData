# AudioBookData

This simple Python program takes sales data from Spotify For Authors for audiobooks, which are given monthly in a CSV file, parses the data, then displays it in a chart using pyplot.
